1.运行程序时在matlab里运行hj_testMain主函数即可
2.被试可通过上下左右（箭头）来调整图片某侧的白点，按空格键即可调整另一侧图片颜色，通过(alt/ctrl/shift)+上下左右键可增大调色的力度。图片初始白点为6500k色温色度点。调色逻辑是白点在1976uv色品图上以起点根据上下左右键进行uv坐标的变化。调色完成后按下“enter”键进行保存。
3.实验结果被保存在keyboard.uvf变量中，并保存在uv.txt文本文档中，左侧为左侧光源适应点，右侧为右侧光源适应点。